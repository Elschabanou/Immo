This folder contains a minimal Next.js frontend converted from the existing Jinja templates.

Quick start (development):

1. cd frontend
2. npm install
3. npm run dev

Build & static export (for Netlify):

1. cd frontend
2. npm ci
3. npm run build

Set `NEXT_PUBLIC_API_BASE` to the full API URL if the backend is hosted on a different domain.
